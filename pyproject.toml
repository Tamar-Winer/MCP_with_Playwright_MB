[project]
name = "weather-mcp"
version = "0.1.0"
description = "Weather MCP Servers - USA via NWS API, Israel via Playwright"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "anthropic>=0.40.0",
    "playwright>=1.40.0",
    "httpx>=0.27.0",
]

[tool.uv]
package = false
